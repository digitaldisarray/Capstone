import java.awt.Color;

public class Colors {
	/*
	 * These are all colors frequently used.
	 * They are here so we don't have to instantiate a new color object every time we need it.
	 */
	
	public static final Color RED = new Color(255, 0, 0);
	public static final Color LIGHT_GREEN = new Color(30, 210, 95);
}
